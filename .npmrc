legacy-peer-deps=true
# This ensures npm can install packages with peer dependency conflicts
# which can sometimes cause failures in CI environments like Vercel

# Handle node-gyp failures for native dependencies
build-from-source=true
unsafe-perm=true
